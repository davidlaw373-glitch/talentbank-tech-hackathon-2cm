import { UniversityAuthForm } from "@/components/features/university/university-auth-form";

export default function UniversityLoginPage() {
  return (
    <main
      id="main-content"
      className="container mx-auto flex min-h-screen items-center justify-center p-6"
    >
      <UniversityAuthForm />
    </main>
  );
}
